<div class="pageTitle">
  <h1>Pictures</h1>
</div>

<div class="toolbar">
  <div class="leftItem regularButton">
    <a href="<%= Rho::RhoConfig.start_path %>">Home</a>
  </div>
  <div class="rightItem regularButton">
    <a class="button" href="<%= url_for :action => :new %>">New</a>
  </div>
</div>

<div class="content">
  <ul>
    <% @pictures.each do |picture| %>
    
        <li>
          <a href="<%= url_for :action => :show, :id => picture.object %>">
            <span class="title"><%= picture.image_data %></span><span class="disclosure_indicator"></span>
          </a>
        </li>
    
    <% end %>
  </ul>
</div>